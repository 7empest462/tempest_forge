use bevy::prelude::*;
use bevy::render::render_resource::{AsBindGroup, BindGroupLayout};
use std::any::Any;
use tempest_forge::world::water::WaterMaterial;

fn main() {
    println!("I cannot easily test bind group layouts without RenderDevice.");
}
